# Catalyst Experience API

This api has been developed with golang, jwt, gorm, mysql.

> Rename the file **config.sample.json** as **config.json** and fill all configuration variables before run the api.
